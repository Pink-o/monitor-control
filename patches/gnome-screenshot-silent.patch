diff --git a/src/screenshot-application.c b/src/screenshot-application.c
index 654bc69..00cbc6b 100644
--- a/src/screenshot-application.c
+++ b/src/screenshot-application.c
@@ -264,6 +264,7 @@ save_with_description_and_profile (ScreenshotApplication *self,
                                    save_pixbuf_ready_cb, self,
                                    "icc-profile", self->icc_profile_base64,
                                    "tEXt::Software", "gnome-screenshot",
+                                   "compression", "1",
                                    NULL);
 }
 static void
@@ -276,6 +277,7 @@ save_with_description (ScreenshotApplication *self,
                                    format, NULL,
                                    save_pixbuf_ready_cb, self,
                                    "tEXt::Software", "gnome-screenshot",
+                                   "compression", "1",
                                    NULL);
 }
 
@@ -288,6 +290,7 @@ save_with_no_profile_or_description (ScreenshotApplication *self,
                                    G_OUTPUT_STREAM (os),
                                    format, NULL,
                                    save_pixbuf_ready_cb, self,
+                                   "compression", "1",
                                    NULL);
 }
 
@@ -504,6 +507,21 @@ finish_take_screenshot (ScreenshotApplication *self)
 
   self->screenshot = screenshot;
 
+  /* Scale down to 1/4 size for faster processing */
+  if (self->screenshot != NULL) {
+    int w = gdk_pixbuf_get_width (self->screenshot);
+    int h = gdk_pixbuf_get_height (self->screenshot);
+    int new_w = w / 4;
+    int new_h = h / 4;
+    if (new_w > 10 && new_h > 10) {
+      GdkPixbuf *scaled = gdk_pixbuf_scale_simple (self->screenshot, new_w, new_h, GDK_INTERP_NEAREST);
+      if (scaled != NULL) {
+        g_object_unref (self->screenshot);
+        self->screenshot = scaled;
+      }
+    }
+  }
+
   if (screenshot_config->copy_to_clipboard)
     {
       screenshot_save_to_clipboard (self);
diff --git a/src/screenshot-backend-shell.c b/src/screenshot-backend-shell.c
index 84c5ead..4c7672d 100644
--- a/src/screenshot-backend-shell.c
+++ b/src/screenshot-backend-shell.c
@@ -60,7 +60,7 @@ screenshot_backend_shell_get_pixbuf (ScreenshotBackend *backend,
       method_params = g_variant_new ("(bbbs)",
                                      TRUE,
                                      screenshot_config->include_pointer,
-                                     TRUE, /* flash */
+                                     FALSE, /* flash */
                                      filename);
     }
   else if (rectangle != NULL)
@@ -69,7 +69,7 @@ screenshot_backend_shell_get_pixbuf (ScreenshotBackend *backend,
       method_params = g_variant_new ("(iiiibs)",
                                      rectangle->x, rectangle->y,
                                      rectangle->width, rectangle->height,
-                                     TRUE, /* flash */
+                                     FALSE, /* flash */
                                      filename);
     }
   else
@@ -77,7 +77,7 @@ screenshot_backend_shell_get_pixbuf (ScreenshotBackend *backend,
       method_name = "Screenshot";
       method_params = g_variant_new ("(bbs)",
                                      screenshot_config->include_pointer,
-                                     TRUE, /* flash */
+                                     FALSE, /* flash */
                                      filename);
     }
 
